async function copy(text){if(navigator.clipboard&&window.isSecureContext){await navigator.clipboard.writeText(text);return}const input=document.createElement('textarea');input.value=text;input.style.position='fixed';input.style.opacity='0';document.body.appendChild(input);input.select();document.execCommand('copy');input.remove()}document.querySelectorAll('.copy-path').forEach(button=>button.addEventListener('click',async()=>{try{await copy(button.dataset.path);button.textContent='已复制'}catch{button.textContent='请手动复制路径'}setTimeout(()=>button.textContent='复制路径',1600)}));
