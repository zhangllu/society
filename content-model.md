# 内容模型

## 1. 用途

本文定义第一版网站需要的内容对象、字段和可见范围。

原则：

> 用户看到生活问题、智者之眼、材料、场景和下一步；内部保留分类、字段和技术结构。

## 2. 对象总览

第一版只保留五类内容对象：

```text
Entry 生活入口
Thinker 智者之眼
Way 看见方式
Work 作品
Concept 概念
```

其中：

- `Entry` 是首页入口，面向用户。
- `Thinker` 是核心内容，面向用户。
- `Way` 是“看见方式”，面向用户，但不在首页用理论词。
- `Work` 第一版只挂在智者页，不单独成页。
- `Concept` 第一版只作为解释工具，不单独成词典。

## 3. Entry：生活入口

### 3.1 作用

把用户熟悉的生活问题，连接到相关智者。

用户看到的是：

```text
规则与组织
金钱与机会
权力与制度
关系与人情
判断与选择
文化与差异
```

内部再映射到更系统的分类。

### 3.2 字段

| 字段 | 说明 | 用户可见 |
|---|---|---|
| `id` | 唯一标识 | 否 |
| `name` | 入口名称 | 是 |
| `question` | 入口问题 | 是 |
| `summary` | 一句话说明 | 是 |
| `scenes` | 用户可能遇到的场景 | 是 |
| `thinkers` | 推荐智者 ID | 是，显示为智者卡 |
| `backend_domains` | 内部问题分类 | 否 |
| `related_entries` | 相关入口 | 是 |

### 3.3 用户侧呈现

不要写：

```text
本入口对应政治博弈和社会交换。
```

要写：

```text
当规则决定谁能行动、谁能说话、谁必须服从时，你已经进入了这个问题。
```

## 4. Thinker：智者之眼

### 4.1 作用

每位智者不是百科人物，而是一双可借用的眼睛。

智者页要回答：

```text
这双眼睛从哪里来？
他从什么材料中看社会？
他如何形成概念？
今天借他能看见什么？
这双眼睛看不见什么？
```

### 4.2 字段

| 字段 | 说明 | 用户可见 |
|---|---|---|
| `id` | 唯一标识 | 否 |
| `name` | 中文名 | 是 |
| `name_en` | 英文名 | 可选 |
| `years` | 生卒年 | 可选 |
| `status` | 已完成 / 建设中 / 待建设 | 是 |
| `eye` | 一双什么样的眼睛 | 是 |
| `entry_question` | 他从什么问题切入 | 是 |
| `entry_points` | 他从哪里看社会 | 是 |
| `life_context` | 人生或时代处境 | 是 |
| `materials` | 使用的材料 | 是 |
| `working_method` | 做学问方式 | 是 |
| `concept_birth` | 一个概念如何出生 | 是 |
| `concepts` | 关键概念 | 是，但需要解释 |
| `ways` | 内部看见方式标注 | 可在详情页弱展示 |
| `applications` | 借他看今天 | 是 |
| `blind_spot` | 盲区 | 是 |
| `works` | 入门与核心作品 | 是 |
| `related` | 可继续看的智者 | 是 |

### 4.3 用户侧呈现

不要写：

```text
核心概念：科层制、理性化、合法权威。
```

要写：

```text
他注意到，现代组织越来越依赖职位、文书、流程和规则。后来我们可以用“科层制”来概括这种组织形态。
```

## 5. Way：看见方式

### 5.1 作用

帮助用户理解：不同智者不是只有不同观点，也有不同看社会的方式。

第一版用户侧名称叫：

```text
看见方式
```

不优先使用：

```text
认知方式
```

### 5.2 字段

| 字段 | 说明 | 用户可见 |
|---|---|---|
| `id` | 唯一标识 | 否 |
| `name` | 用户侧名称 | 是 |
| `internal_name` | 内部理论名称 | 可选 |
| `how_it_sees` | 它如何看社会 | 是 |
| `materials` | 通常使用什么材料 | 是 |
| `good_at` | 擅长看见什么 | 是 |
| `blind_spot` | 容易遮蔽什么 | 是 |
| `thinkers` | 代表智者 | 是 |

### 5.3 用户侧示例

```text
进入现场，看人们真实如何生活

它不是先坐在书房里下定义，而是进入具体现场，观察人们如何称呼彼此、交换礼物、处理冲突、解释规则。
```

内部可以标注：

```text
internal_name: 田野调查
```

## 6. Work：作品

### 6.1 作用

作品是智者之眼的证据。

第一版不做独立作品页，只在智者页中呈现阅读路径。

### 6.2 字段

| 字段 | 说明 | 用户可见 |
|---|---|---|
| `title` | 作品名 | 是 |
| `role` | 入门 / 核心 / 深入 | 是 |
| `why_read` | 为什么读这本 | 是 |
| `related_concepts` | 关联概念 | 可选 |

### 6.3 用户侧呈现

不要写：

```text
代表作：《经济与社会》。
```

要写：

```text
深入读：《经济与社会》
这本书适合用来系统理解社会行动、权威和组织形式。
```

## 7. Concept：概念

### 7.1 作用

概念不是死标签，而是智者把经验压缩成的工具。

第一版不做概念词典，只在智者页中解释“一个概念如何出生”。

### 7.2 字段

| 字段 | 说明 | 用户可见 |
|---|---|---|
| `name` | 概念名 | 是 |
| `plain_explanation` | 白话解释 | 是 |
| `from_problem` | 它从什么问题中来 | 是 |
| `from_material` | 它从什么材料中来 | 是 |
| `used_by` | 哪些智者使用 | 可选 |

### 7.3 用户侧呈现

不要写：

```text
理性化：现代性的核心过程。
```

要写：

```text
韦伯注意到，越来越多事情不再靠习惯、人情或个人判断，而是靠计算、流程和规则来安排。这个变化后来可以叫作“理性化”。
```

## 8. 字段可见性规则

### 8.1 用户可见

- 标题
- 场景
- 问题
- 这双眼睛适合看什么
- 人生或时代处境
- 使用的材料
- 工作方式
- 概念如何出生
- 今天能看见什么
- 盲区
- 下一步读什么

### 8.2 内部使用

- 后台分类
- 数据字段
- 页面路径
- 技术格式
- 理论标签
- 后续接口

## 9. 第一版数据文件建议

可以先用一个 `data-sample.json` 验证结构。

后续正式开发时可拆为：

```text
data/entries.json
data/thinkers.json
data/ways.json
```

