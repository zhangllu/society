# 工作流原则

## 1. 核心原则：先看见，再批量

任何可以批量复制的内容，都必须先做一个最小样板。

只有当样板已经放到网页中真实呈现，并经过确认后，才允许继续批量制作同类内容。

一句话：

> 没有在网页里看见并确认，就不要大规模做同样的内容。

## 2. 为什么需要这个原则

这个项目不是普通资料整理，而是一个重视呈现方式、可供性和场域感的网站。

很多内容在文档里看起来成立，但放到网页上可能会出现问题：

- 文案太长；
- 入口太硬；
- 卡片没有点击欲望；
- 模块过多；
- 字段看似完整，但用户不需要；
- 术语解释仍然像知识课；
- 页面节奏不对；
- 内容批量生成后发现方向偏了。

因此，不能先批量生产，再回头调整。

第一版必须采用：

```text
单样板 → 网页呈现 → 人工确认 → 再批量
```

## 3. 适用范围

凡是可复制模块，都适用本原则。

包括：

- 首页第一屏；
- 一个生活入口页；
- 一张智者卡片；
- 一个智者详情页；
- 一个看见方式卡片；
- 一个概念如何出生模块；
- 一个作品阅读路径模块；
- 一个“换一双眼睛”模块；
- 一个待建设卡片；
- 一个页面底部继续探索区域。

## 4. 标准流程

### 4.1 第一步：只做一个样板

每类模块先只做一个。

建议第一批样板：

```text
生活入口：规则与组织
智者：韦伯
看见方式：进入现场，看人们真实如何生活
概念模块：科层制如何出生
```

### 4.2 第二步：放进网页

必须进入真实页面，而不是只看 Markdown 或 JSON。

检查：

- 首屏是否自然；
- 文案是否太长；
- 用户是否知道下一步点哪里；
- 是否出现用户看不懂的黑话；
- 卡片是否有可供性；
- 页面是否像知识库，还是像可探索场域。

### 4.3 第三步：人工确认

确认标准：

```text
这个模块放在网页上是成立的。
它的语言、节奏、信息量和交互方向都可以继续复用。
```

没有确认，不进入批量。

### 4.4 第四步：再批量生产

确认后，才允许扩展：

- 从 1 个入口扩展到 6 个入口；
- 从 1 位智者扩展到 6 位样板智者；
- 从 1 张卡片扩展到 60 位智者索引；
- 从 1 个看见方式扩展到多种看见方式；
- 从 1 个概念模块扩展到更多概念。

## 5. 明确禁止

第一版禁止以下做法：

- 还没看网页效果，就一次性写完 6 位智者；
- 还没确认智者卡片，就批量生成 60 张卡；
- 还没确认入口页逻辑，就扩展 6 个入口；
- 还没确认详情页结构，就写完整人物库；
- 还没确认一个概念模块，就批量整理概念；
- 还没确认页面节奏，就开始做图谱、资源库或复杂交互。

## 6. 样板确认清单

每个样板上线后，至少检查以下问题：

```text
1. 普通用户能不能看懂？
2. 有没有没解释的黑话？
3. 用户知道下一步可以点哪里吗？
4. 这个模块是否有“借一双眼睛”的感觉？
5. 它是否比静态知识条目更有场域感？
6. 信息量是否刚好，而不是过载？
7. 这个样式是否值得复制到同类内容？
```

只有以上问题基本通过，才允许批量。

## 7. 第一阶段建议执行顺序

```text
1. 做首页第一屏样板
2. 做“规则与组织”入口页样板
3. 做“韦伯”智者卡片样板
4. 做“韦伯”详情页样板
5. 做一个看见方式卡片样板
6. 网页确认
7. 再扩展其他入口和其他智者
```

## 8. 一句话纪律

> 所有批量化之前，都必须先有一个被网页验证过的样板。

